name: adreward_attribution_example
description: Minimal example of integrating the AdReward attribution SDK.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  adreward_attribution:
    path: ../
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_lints: ^4.0.0
